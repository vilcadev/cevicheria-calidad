import { NoSpecialCharDirective } from './no-special-char.directive';

describe('NoSpecialCharDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpecialCharDirective();
    expect(directive).toBeTruthy();
  });
});
