import { DecimalNumbersDirective } from './decimal-numbers.directive';

describe('DecimalNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
